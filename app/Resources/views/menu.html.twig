<ul class="nav navbar-nav">

    {% if has_right('IS_AUTHENTICATED_REMEMBERED') %}
        <li>
            <a href="{{ path('app_homepage') }}"><span class="glyphicon glyphicon-home"></span> Home</a>
        </li>

        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
               aria-expanded="false">Computers <span class="caret"></span></a>
            <ul class="dropdown-menu">
                <li><a href="{{ path('computer') }}">List</a></li>
            </ul>
        </li>

        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
               aria-expanded="false">Persons <span class="caret"></span></a>
            <ul class="dropdown-menu">
                <li><a href="{{ path('person') }}">List</a></li>
            </ul>
        </li>

    {% endif %}

</ul>
<ul class="nav navbar-right">
    <li class="">
        {% if has_right('IS_AUTHENTICATED_REMEMBERED') %}
            <a href="{{ path('logout') }}">{{ app.user.username }} {{ 'security.log.out' | trans }}</a>



        {% else %}
            <a href="{{ path('login') }}">{{ 'security.sign.in' | trans }}</a>
        {% endif %}
    </li>
</ul>

